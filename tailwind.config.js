// Tailwind v4 uses CSS-first config via the @theme block.
// All design tokens (colors, typography, shadows, radius, animations)
// live in src/app/globals.css. This file is kept only so editor
// tooling/IntelliSense can resolve a config path; it is not loaded
// by @tailwindcss/postcss unless explicitly referenced via @config.
module.exports = {}
